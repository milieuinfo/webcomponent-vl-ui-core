import {vlElement, nativeVlElement, define, awaitScript, awaitUntil} from '/src/vl-core.js';
import {VlText} from '/src/vl-text.js';

const VlElement = vlElement;
const NativeVlElement = nativeVlElement;

export {
  vlElement,
  VlElement,
  nativeVlElement,
  NativeVlElement,
  define,
  awaitScript,
  awaitUntil,
  VlText,
};
